// The menu stucture that contains main pages: story, My tree, My profile, Add member, edit member, edit Tree
import Foundation

public enum MenuTab {
    case story, myTree, myProfile, addMember, editMember, editTree
} 
